keys:
  devkey: secret

redis:
  address: localhost:6379

rtc:
  tcp_port: 7881
  udp_port: 7882
  use_external_ip: true
  enable_loopback_candidate: false

turn:
  enabled: true
  domain: localhost
  tls_port: 0
  udp_port: 3478

webhook:
  api_key: devkey
  urls:
    - http://localhost:4000/api/v1/livekit/webhook

